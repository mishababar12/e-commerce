// src/app/cart/page.tsx
"use client";  // Ensures this page is client-side

import CartPage from "../../app/components/cartPage"; // Adjust the path based on where your CartPage component is

const CartPageWrapper = () => {
  return <CartPage />;
};

export default CartPageWrapper;
